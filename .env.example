VITE_GCENTRO_SUPABASE_URL=https://your-project-id.supabase.co
VITE_GCENTRO_SUPABASE_ANON_KEY=your-anon-public-key-here

# ── Sentry (reporte de errores) ──────────────────────────────────────────────
# Todas OPCIONALES: sin DSN, Sentry queda desactivado y la app funciona igual.
# Sentry SOLO se inicializa en builds de producción (nunca en dev).

# DSN del proyecto. Es público por diseño (viaja en el bundle del navegador).
VITE_SENTRY_DSN=
# production | staging — etiqueta los eventos por ambiente.
VITE_SENTRY_ENVIRONMENT=production
# Versión del release, para atribuir errores a un deploy.
VITE_SENTRY_RELEASE=

# ── Solo para el BUILD (sin prefijo VITE_: no entran al bundle) ──────────────
# ⚠️ SENTRY_AUTH_TOKEN es un SECRETO. Va en las env vars del hosting, NUNCA acá
#    ni en ningún archivo versionado. Sin él, el build no sube source maps.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
